services:
  - type: cron
    name: switch2-stock-checker
    runtime: python
    schedule: "*/1 * * * *"
    buildCommand: pip install -r requirements.txt
    startCommand: python check_stock.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
